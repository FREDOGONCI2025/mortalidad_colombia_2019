services:
  - type: web
    name: mortalidad-app
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10